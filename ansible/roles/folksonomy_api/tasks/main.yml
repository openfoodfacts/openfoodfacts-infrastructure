# Install folksonomy engine
---
- name: Run install process
  ansible.builtin.include_tasks:
    file: install.yml
  tags: install

- name: Run deployment
  ansible.builtin.include_tasks:
    file: deploy.yml
  tags: ["install", "deploy"]
