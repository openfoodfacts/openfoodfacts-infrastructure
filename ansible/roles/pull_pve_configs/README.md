# Pull PVE Configs

This role pulls pve config using the pseudo filesystem at /etc/pve.

This is a simple way to backup cluster settings.

FIXME:

exclusion: *.key or use of git-crypt
 priv/ folder
 token.cfg
consider: shadow.cfg ssh_known_hosts authorized_keys
